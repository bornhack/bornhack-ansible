all:
  children:
    freebsd_hosts:
      hosts:
        container1.servers.bornhack.org:
        nuc1.servers.bornhack.org:

    jail_hosts:
      hosts:
        nuc1.servers.bornhack.org:

    nuc1_jails:
      hosts:
        alertmanager2.servers.bornhack.org:
        ansible3.servers.bornhack.org:
        blackbox2.servers.bornhack.org:
        django4.servers.bornhack.org: # staging website
        django5.servers.bornhack.org: # prod website
        django6.servers.bornhack.org: # staging bma
        django7.servers.bornhack.org: # prod bma
        dns1.servers.bornhack.org:
        grafana2.servers.bornhack.org:
        imap3.servers.bornhack.org:
        people3.servers.bornhack.org:
        postgres1.servers.bornhack.org:
        prometheus2.servers.bornhack.org:
        smtp2.servers.bornhack.org:
        syslog1.servers.bornhack.org:
        tor3.servers.bornhack.org:
        webproxy3.servers.bornhack.org:

    alertmanager_servers:
      hosts:
        alertmanager2.servers.bornhack.org:

    ansible_servers:
      hosts:
        ansible3.servers.bornhack.org:

    blackbox_exporter_servers:
      hosts:
        blackbox2.servers.bornhack.org:

    django_channels2_servers:
      hosts:
        django4.servers.bornhack.org: # staging website
        django5.servers.bornhack.org: # prod website
        django6.servers.bornhack.org: # staging bma
        django7.servers.bornhack.org: # prod bma


    django_homepage_servers:
      hosts:
        django4.servers.bornhack.org: # staging
        django5.servers.bornhack.org: # prod

    django_bma_servers:
      hosts:
        django6.servers.bornhack.org: # staging
        django7.servers.bornhack.org: # prod

    dmarc_exporter_servers:
      hosts:
        imap3.servers.bornhack.org:

    dovecot_servers:
      hosts:
        imap3.servers.bornhack.org:

    grafana_servers:
      hosts:
        grafana2.servers.bornhack.org:

    motion_servers:
      hosts:
        container1.servers.bornhack.org:

    nginx_servers:
      hosts:
        webproxy3.servers.bornhack.org:

    nut_servers:
      hosts:
        container1.servers.bornhack.org:

    people_servers:
      hosts:
        people3.servers.bornhack.org:

    postfix_servers:
      hosts:
        smtp2.servers.bornhack.org:

    postgresql_servers:
      hosts:
        postgres1.servers.bornhack.org:

    prometheus_servers:
      hosts:
        prometheus2.servers.bornhack.org:

    syslogng_servers:
      hosts:
        syslog1.servers.bornhack.org:

    tor_servers:
      hosts:
        tor3.servers.bornhack.org:
        container1.servers.bornhack.org:

    unbound_servers:
      hosts:
        dns1.servers.bornhack.org:
